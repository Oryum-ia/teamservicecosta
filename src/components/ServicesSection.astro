---
// Services Section
const services = [
  {
    icon: 'wrench',
    title: 'Mantenimiento y Reparación',
    description: 'Optimiza tu equipo con nuestro mantenimiento y servicio de reparación experto. Técnicos certificados KÄRCHER con experiencia en todo tipo de equipos de limpieza.',
    features: ['Diagnóstico profesional', 'Repuestos originales', 'Garantía de servicio']
  },
  {
    icon: 'truck',
    title: 'Distribución de Equipos',
    description: 'Accede a equipos de alta calidad a través de nuestra distribución confiable. Somos distribuidores autorizados KÄRCHER en la región Caribe.',
    features: ['Productos originales', 'Entrega inmediata', 'Stock disponible']
  },
  {
    icon: 'lightbulb',
    title: 'Asesoría Técnica',
    description: 'Recibe asesoramiento profesional para elegir y utilizar tus equipos de manera óptima. Te ayudamos a encontrar la solución perfecta para tus necesidades.',
    features: ['Asesoría personalizada', 'Recomendaciones expertas', 'Soporte continuo']
  }
];

// SVG icons mapping
const iconSVGs = {
  wrench: '<path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>',
  truck: '<path d="M1 3h15v13H1z"/><path d="M16 8h3l3 3v5h-4"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/>',
  lightbulb: '<path d="M9 21h6"/><path d="M12 3v3"/><path d="m4.22 4.22 1.42 1.42"/><path d="M18.36 5.64l1.42-1.42"/><path d="M1 12h3"/><path d="M20 12h3"/><path d="M4.22 19.78l1.42-1.42"/><path d="M18.36 18.36l1.42 1.42"/><circle cx="12" cy="12" r="5"/>'
};
---

<section id="servicios" class="services-section section bg-gray">
  <div class="container">
    <div class="section-header">
      <div class="section-badge">
        <span class="badge-icon">✓</span>
        Centro Autorizado KÄRCHER
      </div>
      <h2 class="section-title">Nuestros Servicios</h2>
      <p class="section-description">
        Soluciones técnicas, capacitación y soporte profesional con el sello de calidad Team Service Costa.
      </p>
    </div>

    <div class="services-grid">
      {services.map(service => (
        <div class="service-card">
          <div class="service-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon-svg" set:html={iconSVGs[service.icon as keyof typeof iconSVGs]}>
            </svg>
          </div>
          <h3 class="service-title">{service.title}</h3>
          <p class="service-description">{service.description}</p>
          <ul class="service-features">
            {service.features.map(feature => (
              <li class="feature-item">
                <svg class="check-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
                {feature}
              </li>
            ))}
          </ul>
          <button class="service-btn" data-service-title={service.title}>
            Más Información
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14"></path>
              <path d="m12 5 7 7-7 7"></path>
            </svg>
          </button>
        </div>
      ))}
    </div>

    <div class="cta-banner">
      <div class="cta-content">
        <div class="cta-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
          </svg>
        </div>
        <div class="cta-text">
          <h3 class="cta-title">¿Necesitas reparar tu equipo?</h3>
          <p class="cta-description">
            Agenda una cita o consulta el estado de tu reparación en tiempo real
          </p>
        </div>
        <div class="cta-actions">
          <a href="#contacto" class="btn-cta-primary">Agendar Servicio</a>
          <a href="/estado-producto" class="btn-cta-secondary">Ver Mi Reparación</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Service Info Modal -->
<div id="service-info-modal" class="service-modal" aria-hidden="true">
  <div class="service-modal-overlay"></div>
  <div class="service-modal-content">
    <button class="service-modal-close" aria-label="Cerrar">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>

    <div class="service-modal-body">
      <div class="service-modal-icon" id="modal-service-icon">🔧</div>
      <h2 class="service-modal-title" id="modal-service-title">Título del Servicio</h2>
      <div class="service-modal-description" id="modal-service-description">
        Descripción del servicio
      </div>
      <div class="service-modal-actions">
        <a href="#" id="modal-whatsapp-btn" class="btn-modal-whatsapp" target="_blank">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path>
          </svg>
          Contactar por WhatsApp
        </a>
      </div>
    </div>
  </div>
</div>

<style>
  .services-section {
    background: var(--color-gray-50);
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto var(--spacing-12);
  }

  .section-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-2);
    background: #FFD700;
    border: 1px solid #FFD700;
    color: #1a1a2e;
    padding: var(--spacing-2) var(--spacing-4);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    margin-top: var(--spacing-6);
    margin-bottom: var(--spacing-6);
  }

  .badge-icon {
    font-size: var(--text-base);
  }

  .section-title {
    font-size: var(--text-4xl);
    color: #1a1a2e;
    margin-bottom: var(--spacing-4);
  }

  .section-description {
    font-size: var(--text-lg);
    color: var(--color-gray-600);
    line-height: 1.7;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-8);
    margin-bottom: var(--spacing-12);
  }

  .service-card {
    background: var(--color-white);
    padding: var(--spacing-8);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
    position: relative;
    overflow: hidden;
  }

  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #FFD700 0%, #FFC700 100%);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--transition-base);
  }

  .service-card:hover::before {
    transform: scaleX(1);
  }

  .service-card:hover {
    box-shadow: var(--shadow-xl);
    transform: translateY(-4px);
  }

  .service-icon {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, rgba(255, 215, 0, 0.1) 0%, rgba(255, 215, 0, 0.2) 100%);
    border-radius: var(--radius-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--spacing-6);
  }

  .icon-svg {
    color: #FFD700;
  }

  .service-title {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: #1a1a2e;
    margin-bottom: var(--spacing-3);
  }

  .service-description {
    color: var(--color-gray-600);
    line-height: 1.7;
    margin-bottom: var(--spacing-6);
  }

  .service-features {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--spacing-6) 0;
    flex: 1;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
    color: var(--color-gray-700);
    font-size: var(--text-sm);
    margin-bottom: var(--spacing-2);
  }

  .check-icon {
    color: #FFD700;
    flex-shrink: 0;
  }

  .service-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-2);
    padding: var(--spacing-3) var(--spacing-6);
    background: transparent;
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    color: #1a1a2e;
    font-weight: var(--font-semibold);
    font-size: var(--text-sm);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .service-btn:hover {
    background: #FFD700;
    border-color: #FFD700;
    color: #1a1a2e;
    transform: translateY(-2px);
  }

  /* CTA Banner */
  .cta-banner {
    background: linear-gradient(135deg, #1a1a2e 0%, #2d2d44 100%);
    border-radius: var(--radius-2xl);
    padding: var(--spacing-10);
    box-shadow: var(--shadow-xl);
    position: relative;
    overflow: hidden;
  }

  .cta-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 80% 50%, rgba(255, 215, 0, 0.1) 0%, transparent 60%);
    pointer-events: none;
  }

  .cta-content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: var(--spacing-8);
    align-items: center;
  }

  .cta-icon {
    color: #FFD700;
  }

  .cta-text {
    flex: 1;
  }

  .cta-title {
    color: var(--color-white);
    font-size: var(--text-2xl);
    margin-bottom: var(--spacing-2);
  }

  .cta-description {
    color: var(--color-gray-300);
    margin: 0;
  }

  .cta-actions {
    display: flex;
    gap: var(--spacing-4);
  }

  .btn-cta-primary,
  .btn-cta-secondary {
    padding: var(--spacing-4) var(--spacing-6);
    border-radius: var(--radius-lg);
    font-weight: var(--font-semibold);
    font-size: var(--text-base);
    text-decoration: none;
    transition: all var(--transition-base);
    white-space: nowrap;
  }

  .btn-cta-primary {
    background: #FFD700;
    color: #1a1a2e;
  }

  .btn-cta-primary:hover {
    background: #FFC700;
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(255, 215, 0, 0.3);
  }

  .btn-cta-secondary {
    background: transparent;
    color: var(--color-white);
    border: 2px solid var(--color-white);
  }

  .btn-cta-secondary:hover {
    background: var(--color-white);
    color: #1a1a2e;
  }

  /* Responsive Design */
  @media (max-width: 968px) {
    .services-grid {
      grid-template-columns: 1fr;
    }

    .section-title {
      font-size: var(--text-3xl);
    }

    .cta-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--spacing-6);
    }

    .cta-icon {
      margin: 0 auto;
    }

    .cta-actions {
      flex-direction: column;
    }

    .btn-cta-primary,
    .btn-cta-secondary {
      width: 100%;
      text-align: center;
    }
  }

  @media (min-width: 640px) and (max-width: 968px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Service Modal Styles */
  .service-modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 10000;
    display: none;
    pointer-events: none;
  }

  .service-modal[aria-hidden="false"] {
    display: block;
    pointer-events: auto;
  }

  .service-modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0);
    transition: background 0.3s ease;
    backdrop-filter: blur(0px);
  }

  .service-modal[aria-hidden="false"] .service-modal-overlay {
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(8px);
  }

  .service-modal-content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%) scale(0.9);
    width: 90%;
    max-width: 600px;
    max-height: 90vh;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    opacity: 0;
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    overflow: hidden;
  }

  .service-modal[aria-hidden="false"] .service-modal-content {
    transform: translate(-50%, -50%) scale(1);
    opacity: 1;
  }

  .service-modal-close {
    position: absolute;
    top: var(--spacing-3);
    right: var(--spacing-3);
    z-index: 10;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(10px);
    border: none;
    border-radius: var(--radius-full);
    color: var(--color-white);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .service-modal-close svg {
    width: 18px;
    height: 18px;
  }

  .service-modal-close:hover {
    background: rgba(0, 0, 0, 0.8);
    transform: rotate(90deg);
  }

  .service-modal-body {
    padding: var(--spacing-5);
    text-align: center;
    overflow: hidden;
  }

  .service-modal-icon {
    font-size: 2.5rem;
    margin-bottom: var(--spacing-2);
    animation: bounce-in 0.6s ease-out;
  }

  @keyframes bounce-in {
    0% {
      transform: scale(0);
    }
    50% {
      transform: scale(1.1);
    }
    100% {
      transform: scale(1);
    }
  }

  .service-modal-title {
    font-size: var(--text-lg);
    font-weight: var(--font-bold);
    color: #1a1a2e;
    margin-bottom: var(--spacing-2);
    line-height: 1.2;
  }

  .service-modal-description {
    font-size: 13px;
    color: var(--color-gray-700);
    line-height: 1.5;
    text-align: left;
    margin-bottom: var(--spacing-3);
    white-space: pre-line;
    max-height: 50vh;
    overflow-y: auto;
  }

  .service-modal-actions {
    display: flex;
    justify-content: center;
    padding-top: var(--spacing-2);
    border-top: 1px solid var(--color-gray-200);
  }

  .btn-modal-whatsapp {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 10px var(--spacing-4);
    background: #25D366;
    color: var(--color-white);
    border: none;
    border-radius: var(--radius-md);
    font-size: 13px;
    font-weight: var(--font-semibold);
    text-decoration: none;
    cursor: pointer;
    transition: all var(--transition-base);
    box-shadow: 0 4px 14px rgba(37, 211, 102, 0.3);
  }

  .btn-modal-whatsapp svg {
    width: 16px;
    height: 16px;
  }

  .btn-modal-whatsapp:hover {
    background: #20BA5A;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(37, 211, 102, 0.4);
  }

  @media (max-width: 640px) {
    .service-modal-content {
      width: 95%;
      max-height: 85vh;
    }

    .service-modal-body {
      padding: var(--spacing-4);
    }

    .service-modal-icon {
      font-size: 2rem;
      margin-bottom: 8px;
    }

    .service-modal-title {
      font-size: var(--text-base);
      margin-bottom: 8px;
    }

    .service-modal-description {
      font-size: 11px;
      line-height: 1.5;
      margin-bottom: 12px;
      max-height: 40vh;
    }

    .btn-modal-whatsapp {
      font-size: 11px;
      padding: 8px var(--spacing-3);
    }

    .btn-modal-whatsapp svg {
      width: 14px;
      height: 14px;
    }
  }

  /* Scrollbar for description only */
  .service-modal-description::-webkit-scrollbar {
    width: 4px;
  }

  .service-modal-description::-webkit-scrollbar-track {
    background: transparent;
  }

  .service-modal-description::-webkit-scrollbar-thumb {
    background: #d1d5db;
    border-radius: 2px;
  }

  .service-modal-description::-webkit-scrollbar-thumb:hover {
    background: #9ca3af;
  }
</style>

<script>
  // Service information details
  const serviceDetails = {
    'Mantenimiento y Reparación': {
      icon: '🔧',
      fullDescription: `Nuestro servicio de mantenimiento y reparación está diseñado para mantener tus equipos KÄRCHER en óptimas condiciones.

Incluye:
• Revisión completa del equipo
• Limpieza profunda de componentes internos
• Reemplazo de partes desgastadas con repuestos originales
• Pruebas de funcionamiento y calibración
• Garantía de 6 meses en todas las reparaciones

Contamos con técnicos certificados por KÄRCHER y un taller completamente equipado con herramientas especializadas. Todos nuestros servicios incluyen un informe técnico detallado.`,
      phone: '3205085531',
      whatsappMessage: 'Hola, me gustaría agendar un servicio de mantenimiento y reparación para mi equipo KÄRCHER.'
    },
    'Distribución de Equipos': {
      icon: '🚚',
      fullDescription: `Como distribuidores autorizados KÄRCHER en la región Caribe, ofrecemos la línea completa de productos.

Ventajas de comprar con nosotros:
• Productos 100% originales con garantía oficial
• Precios competitivos y promociones especiales
• Stock disponible de los modelos más populares
• Entrega inmediata en nuestras sedes
• Asesoría especializada para elegir el equipo correcto
• Servicio post-venta y soporte técnico

Financiamiento disponible. Consulta nuestras opciones de pago y planes especiales para empresas.`,
      phone: '3205085531',
      whatsappMessage: 'Hola, estoy interesado en adquirir un equipo KÄRCHER. ¿Podrían darme más información?'
    },
    'Asesoría Técnica': {
      icon: '💡',
      fullDescription: `Nuestro equipo de expertos está disponible para ayudarte a encontrar la solución perfecta para tus necesidades de limpieza.

Servicios de asesoría:
• Evaluación de necesidades específicas
• Recomendación de equipos según tipo de superficie y uso
• Capacitación en uso correcto y mantenimiento preventivo
• Cálculo de costos operativos y retorno de inversión
• Demostraciones de productos
• Soporte técnico continuo

Ya sea para uso doméstico, comercial o industrial, te ayudamos a maximizar tu inversión con la solución KÄRCHER adecuada.`,
      phone: '3205085531',
      whatsappMessage: 'Hola, necesito asesoría técnica para elegir el equipo KÄRCHER adecuado para mis necesidades.'
    }
  };

  // Modal elements
  const modal = document.getElementById('service-info-modal');
  const modalOverlay = modal?.querySelector('.service-modal-overlay');
  const modalClose = modal?.querySelector('.service-modal-close');
  const modalIcon = document.getElementById('modal-service-icon');
  const modalTitle = document.getElementById('modal-service-title');
  const modalDescription = document.getElementById('modal-service-description');
  const modalWhatsappBtn = document.getElementById('modal-whatsapp-btn');

  // Close modal function
  function closeServiceModal() {
    modal?.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';
  }

  // Open modal function
  function openServiceModal(serviceTitle: string) {
    const service = serviceDetails[serviceTitle as keyof typeof serviceDetails];
    if (!service) return;

    // Update modal content
    if (modalIcon) modalIcon.textContent = service.icon;
    if (modalTitle) modalTitle.textContent = serviceTitle;
    if (modalDescription) modalDescription.textContent = service.fullDescription;

    // Update WhatsApp button
    if (modalWhatsappBtn) {
      const encodedMessage = encodeURIComponent(service.whatsappMessage);
      modalWhatsappBtn.setAttribute('href', `https://wa.me/57${service.phone}?text=${encodedMessage}`);
    }

    // Show modal
    modal?.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
  }

  document.addEventListener('DOMContentLoaded', () => {
    // Service buttons click handlers
    const serviceButtons = document.querySelectorAll('.service-btn');
    serviceButtons.forEach(button => {
      button.addEventListener('click', () => {
        const serviceTitle = button.getAttribute('data-service-title');
        if (serviceTitle) {
          openServiceModal(serviceTitle);
        }
      });
    });

    // Modal close handlers
    modalClose?.addEventListener('click', closeServiceModal);
    modalOverlay?.addEventListener('click', closeServiceModal);

    // ESC key to close modal
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal?.getAttribute('aria-hidden') === 'false') {
        closeServiceModal();
      }
    });
  });
</script>
